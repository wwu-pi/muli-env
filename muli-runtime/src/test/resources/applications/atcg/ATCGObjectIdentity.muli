package applications.atcg;
import java.util.HashMap;
import java.util.List;
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.stream.Stream;
import de.wwu.muli.*;
import de.wwu.muli.solution.*;

class ATCGObjectIdentity {

    public static void main(String[] args) {
        Solution<CallableParameters>[] params = (Solution<CallableParameters>[])Muli.getAllSolutions( () -> {
            LinkedList l free;
            l.add(2);
            LinkedList result = Application2UnderTest.modify(l);
            return new CallableParameters(result, l);
        });
    }

}

class Application2UnderTest{
    static LinkedList modify(LinkedList l) {
        l.add("Test");
        return l;
    }
}
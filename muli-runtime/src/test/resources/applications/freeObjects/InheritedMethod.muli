package applications.freeObjects;

import java.util.List;
import de.wwu.muli.*;
import de.wwu.muli.solution.*;
import applications.freeObjects.pojo.*;

public class InheritedMethod {

  public static void main(String[] args) {
    List<Solution<Object>> solutions = Muli.getAllSolutions(InheritedMethod::search);
  }

  private static int search() {
    Shape s free;
    if (s.getArea() == 16) {
      return s.getArea();
    } else {
      throw Muli.fail();
    }
  }
}

package applications.atcg;
import java.util.HashMap;
import java.util.List;
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.stream.Stream;
import de.wwu.muli.*;
import de.wwu.muli.solution.*;

class ATCG {




    public static void main(String[] args) {
        Solution<CallableParameters>[] params = (Solution<CallableParameters>[])Muli.getAllSolutions( () -> {
                    int x free;
                    int result = ApplicationUnderTest.g(ApplicationUnderTest.f(x));
                    return new CallableParameters(result, x);
                });
    }

}

class ApplicationUnderTest{
    static int f(int x) {
        return x * 2;
    }

    static int g(int x) {
        return x/2;
    }

}
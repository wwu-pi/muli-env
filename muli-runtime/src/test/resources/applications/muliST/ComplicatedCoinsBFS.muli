package applications.muliST;

import java.util.List;
import de.wwu.muli.*;
import de.wwu.muli.solution.*;

public class ComplicatedCoinsBFS {

    public static void main(String[] args) {
        new ComplicatedCoinsBFS();
    }

    public ComplicatedCoinsBFS(){
        List<Solution<Object>> solutions = Muli.getAllSolutions(ComplicatedCoinsBFS::searchCoins, SearchStrategy.BreadthFirstSearch);

        for (Solution<Object> solution : solutions) {
            System.out.println(solution.value);
        }
    }

    private static boolean searchCoins() {
        int coin1 free, coin2 free;

        if (coin1 == 0) {
            if (coin2 == 0) {
                return coin1 == 0 || false;
            } else {
                return false;
            }
        } else {
            if (coin2 == 0) {
                return coin1 == 0 && coin2 == 0;
            } else {
                return coin1 == 0 && true;
            }
        }
    }

}

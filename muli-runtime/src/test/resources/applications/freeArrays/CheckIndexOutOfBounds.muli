package applications.freeArrays;
import de.wwu.muli.*;
import de.wwu.muli.solution.*;

public class CheckIndexOutOfBounds {

    public static void main(String[] args) {
        Solution<Object>[] solutions = Muli.getAllSolutions(CheckIndexOutOfBounds::check);
    }

    public class CheckObject {
        public int val;
    }

    private static Boolean check() {
        CheckObject[] objs free;
        if (objs.length > 2) {
            throw Muli.fail();
        }

        Object temp = objs[2]; // Must be out of bounds
        return false;
    }
}